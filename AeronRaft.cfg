SPECIFICATION Spec

\* Basic type checking
\*INVARIANT TypeInvariant

\* These debugging invariants should be VIOLATED to show that the algorithm works.
\* Counterexamples are useful for debugging.
\*INVARIANT Debug_Replication
\*INVARIANT Debug_CompleteElection
\*INVARIANT Debug_StartAnotherElection
\*INVARIANT Debug_AnotherCandidate
\*INVARIANT Debug_AnotherLeader
INVARIANT Debug_CompleteMultipleElections

\* Model values for constants
CONSTANTS
    N1 = N1
    N2 = N2
    N3 = N3
\*    Nodes = { N1, N2, N3 }
    Nodes = { N1, N2 }

    A = A
    B = B
    Payloads = { A, B }

    Null = Null

    \* Maximum length of network queues before loss
    NetworkQueueMaxSize = 1

SYMMETRY Symmetry

CHECK_DEADLOCK FALSE
